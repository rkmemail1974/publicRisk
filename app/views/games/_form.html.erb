<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :turn %><br>
    <%= f.number_field :turn %>
  </div>
  <div class="field">
    <%= f.label :game_state %><br>
    <%= f.number_field :game_state %>
  </div>
  <div class="field">
    <%= f.label :num_players %><br>
    <%= f.number_field :num_players %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
